datasets:
  star: # name of the dataset builder
    # data_dir: ${env.data_dir}/datasets
    data_type: videos # [images|videos|features]
    build_info:
      # Be careful not to append minus sign (-) before split to avoid itemizing
      annotations:
        train:
          url: ./star/train.json
          storage: ./star/train.json
        val:
          url: ./star/val.json
          storage: ./star/val.json
        test:
          url: ./star/val.json
          storage: ./star/val.json
      videos:
        storage: ./star/Charades_v1_480

